# citetrack-prisma-client

Generated Python Prisma client for the citetrack database. Built from
`prisma/prisma/schema/`. Consumed by `apps/api` as a local editable workspace
package — see top-level `prisma/README.md`.
