[project]
name = "citetrack-prisma"
version = "0.1.0"
description = "Citetrack Prisma schema + generated clients (workspace package)"
readme = "README.md"
requires-python = ">=3.13.0"
dependencies = []
